Set Up DB - partially done : Improvements needed
Set Up Serializers
Set Up APIs
Set Up Special Functions(If any - Might be needed to return absolute URL if HyperLinked classes is not used)
Make Migrations && Migrate